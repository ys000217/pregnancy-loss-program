# ONT 工作区三个模块代码与逻辑审查报告

> 审查范围（按用户指定）：① 结构变异(SV)断点提取/liftover 管线；② gsMap/GWAS 多基因信号分析；③ SV 断点 × 甲基化位点联合分析(EWAS)。
> 审查方式：逐文件阅读全部代码，并交叉核对关键产物文件的真实行数/表头/坐标体系。未修改任何文件。

---

## 总览结论

三个模块的代码整体为"探索性/结果导向"脚本，逻辑主干基本正确，但存在 **3 个高危问题**、**若干中危问题**，以及大量可复现性隐患：

1. **模块3 结果文档与当前代码/产物全面脱节**（最高优先级）——文档 `SV_METHYLATION_RESULTS.md` 描述的是"全基因类型"版本的分析，而当前 `make_gene_windows.py` 已改为"仅 protein_coding"，产物数字全部对不上。
2. **模块3 负链基因 up/down 窗口颠倒**——`make_gene_windows.py` 的 5'/3' 侧窗口计算只对正链正确，负链基因（约半数）的上游/下游标签互换，使窗口特异性结论部分失效。
3. **模块2 `gsmap_healthcheck.py` 基因组版本错配**——用 hg38 坐标直接匹配 hg19 参考面板，未做 liftover，产出的 rsID 映射整体错位。
4. 其余为中/低危：meta 权重口径不一致、mean-chi2 中位数双重计数、~34% 断点 liftover 丢失、SV 索引对齐的脆弱性、缺失 GT 静默当非携带者、以及大量硬编码绝对路径。

---

## 模块 1：结构变异(SV)断点提取 / liftover 管线

**涉及文件**：`extract_breakpoints.awk`、`filter_vcf.awk`、`clean_info.awk`、`rename_cn1.awk`、`rename_grch38.awk`、`dedup.sh`、`make_fai.sh`、`run_alignment.sh`、`run_liftover.sh`、`setup_conda.sh`、`paf2chain.py`、`analyze_liftover.py`、`extract_sv_carriers.py`

### 结论
断点坐标转换（VCF 1-based → BED 0-based）、染色体重命名、CIGAR→chain 的 dt/dq 映射等核心逻辑**经逐条推导验证为正确**。主要问题在数据丢失、索引对齐脆弱性和可复现性。

### 发现

| 级别 | 文件/位置 | 问题 | 建议 |
|---|---|---|---|
| 中 | `extract_sv_carriers.py:53` | GT 判型 `if "1" in gt`：`./.`（缺失）被静默判为非携带者，多等位 ALT=2 也会漏判。 | 显式处理 `.` 缺失；用 `gt.split(":")[0]` 精确判 `0/1,1/1`。 |
| 中 | `extract_breakpoints.awk` ↔ `extract_sv_carriers.py` | 两者靠"记录顺序索引 sv_idx"对齐（TRA 也占号）。逻辑正确（已用数据验证：sv_carriers 索引 0–95,634、31 处 TRA 跳跃；bed 索引 7–95,631 为 liftover 后子集），但**脆弱**：只要两个脚本处理的是不同 VCF 或中间有按记录删除的过滤，索引即静默错位，污染整个下游分析。 | 改为用唯一键（如 CHROM:POS:SVTYPE:SVLEN）显式 join，而非依赖隐式顺序索引。 |
| 中 | liftover 环节 | **CN1→GRCh38 丢失约 34% 断点**（推算 145,765 → 实际 `GRCh38_breakpoints.unique.bed` 96,506 行）。断点并非随机丢失，集中于 CN1 与 GRCh38 保守性差的区域，可能系统性偏倚下游 SV×甲基化分析。 | 记录并按 SVTYPE/区域统计丢失率；对丢失断点做敏感性说明。 |
| 低 | `dedup.sh:3` | 按第 4 列（svid）去重而非按坐标；语义依赖 CrossMap 会为同一断点产出重复名。 | 加注释说明去重依据；必要时按 (chrom,start,end) 去重。 |
| 低 | 全部 sh/awk | 硬编码 `/mnt/e/genotype_data/liftover`、`/mnt/d/ONT`、`E:\` 等绝对路径。 | 参数化路径。 |
| 低 | `filter_vcf.awk` | 依赖命令行实参顺序（排除文件在前）。 | 显式注释调用方式。 |
| — | `paf2chain.py` | CIGAR→chain 的 `I→dq`、`D/N→dt` 映射、`-` 链 qStart/qEnd 反转均**验证正确**。方向**已实证确认正确**：断点 `SV2094:L`（CN1 chr1:65,913,036）lift 后为 GRCh38 chr1:65,590,242（偏移 ~323kb，与该区 chain 头 CN1↔GRCh38 约 320kb 坐标差一致）；若方向颠倒输出会是 CN1 坐标本身。 | 可再加一次 round-trip 自测（liftover 已知坐标回推）以防回归。 |
| — | `rename_grch38.awk` / `rename_cn1.awk` | NC 编号→chr、OriSeqID 提取逻辑正确。 | — |

---

## 模块 2：gsMap / GWAS 多基因信号分析

**涉及文件**：`figure3/gwas/*.py`、`figure3/gwas/poll_gsmap.sh`

### 结论
mean χ²/λ_GC 的核心计算（`calc_mean_chisq_fast.py`）正确，meta 主链路（`meta_rpl_ea.py`→`subset_meta_250k.py`）基本自洽。但存在一处基因组版本错配（高危）和若干统计口径问题。

### 发现

| 级别 | 文件/位置 | 问题 | 建议 |
|---|---|---|---|
| **高** | `gsmap_healthcheck.py:27–41,74–77` | 用户 GWAS 是 **hg38**，却直接用 hg38 的 `CHROM_POS` 去匹配 **1000G EUR（hg19）** bim 建成的 `chrom_pos` 键，**未 liftover** → rsID 映射错位；产出的 `combine_gwas_result_v1.gsmap.sumstats.gz` 的 SNP 关联整体错误。 | 先 hg38→hg19 liftover（参照 `meta_rpl_ea.py` 的 `user_hg19.bed` 做法）再映射 rsID；或改用 hg38 参考面板。 |
| 中 | `meta_rpl_ea.py:128` | 权重口径不一致：用户侧 `w_user=sqrt(OBS_CT)`（总样本 N≈608），Sonehara 侧 `w_sone=sqrt(N_eff)`（4·Nc·Nt/(Nc+Nt)）。病例/对照失衡时用户权重被高估。 | 用户侧同样改用 N_eff。 |
| 中 | `meta_rpl_ea.py:115` | `z=BETA/SE` 未处理 `SE==0` → `z=inf`，污染 `z2_list`（mean chi2 可变成 inf）。 | 丢弃/标记 `SE<=0`。 |
| 中 | `meta_rpl_ea.py:116–117,91–92` | 假定 Sonehara `Allele2` 为效应等位基因、`N_case/N_ctrl=1728/24353` 硬编码，均未从文件/文档验证。若实际效应等位基因为 Allele1，Sonehara Z 全体反号。 | 程序化验证效应等位基因约定；从数据列读 N。 |
| 中 | `calc_mean_chisq_abortion.py:50`、`calc_mean_chisq_pval.py:85` | 直方图中位数 `total = n + overflow`，而 `n` 已含 overflow → **overflow SNP 被双重计数**，`median_chi2`/`lambda_GC` 偏高（仅当存在 chi2>200 时显现）。`calc_mean_chisq_fast.py` 无此问题。 | `total` 应等于 `n`。 |
| 中 | `build_boosted_sumstats.py:17,86–88` | 输入 `RPL_combine_gsmap.sumstats.gz` 在本目录任何脚本中都未生成（断链）；且人为把 OLA1 窗口所有 SNP 的 Z 强制写为 8.0（合成信号），与真实结果混在同一目录，易被误用。 | 明确输入生成步骤；合成输出移入 `diagnostic/` 并标注 `SYNTHETIC`，不得作为真实证据。 |
| 低 | `explore_sonehara.py:38` | `mismatch` 死代码；声称的"BETA/SE vs p.value 一致性"未实现。 | 实现或删除。 |
| 低 | `verify_meta.py:47` | `print(f"... p={sr['N_case']}/{sr['N_ctrl']}")` 把样本数打印成 `p=`。 | 改标签为 `N=`。 |
| 低 | `subset_meta_250k.py` | 随机抽样只保证 mean chi2，**不保证保留 OLA1 等目标位点**。 | 分层/强制保留目标区域再随机补齐。 |
| 低 | `poll_gsmap.sh` | 9.5 分钟轮询结束后**静默退出**（无超时/失败标记），日志解析脆弱。 | 加 `set -euo pipefail` 与超时非零退出码。 |
| 低 | `search_gwascat.py` | `size=100` 未分页，`totalElements>100` 会漏。 | 分页遍历。 |

---

## 模块 3：SV 断点 × 甲基化位点联合分析(EWAS)

**涉及文件**：`ewas_engine.py`、`run_sv_ewas.py`、`run_interaction.py`、`build_gene_windows_patients.py`、`make_gene_windows.py`、`parse_gtf.py`、`fdr_select.py`、`extract_matrix_header.ps1`、`dump_clinical.ps1`、`make_report_figures.py` 等

### 结论
回归引擎（OLS 闭式解）、BH-FDR、最近基因映射、窗口重叠等**统计/算法实现经验证正确**。最严重的问题是**结果文档与当前代码脱节**，其次是坐标体系的小边界差异与数据缺失处理。

### 发现

| 级别 | 文件/位置 | 问题 | 建议 |
|---|---|---|---|
| **高** | `make_gene_windows.py:5` 与 `SV_METHYLATION_RESULTS.md` | 代码现改为 `gene_type=="protein_coding"`（产出 20,065 基因），但结果文档仍写"63,086 基因 / 160,104 窗口 / 7,840,645 检验 / 10,483 显著对 / 8,899 位点"。**实测产物**：`genes_windows.tsv`=20,065 基因、`gene_window_patients.tsv`=60,878 窗口、`sv_ewas_results.tsv`=8,560,104 行、`sv_ewas_sig_pairs.tsv`=6,579 对、`sv_ewas_selected_sites.tsv`=5,673 位点。**文档数字与代码/产物全面不一致，无法复现文档结论**。 | 同步更新文档，或在文档中注明"全类型基因"版本与"protein_coding"版本的差异；保留复现旧结果所需的参数/基因集快照。 |
| **高** | `make_gene_windows.py:12–20` | **负链基因 up/down 窗口颠倒**。代码用 `up=[tss-100k,tss]`、`dn=[tes,tes+100k]`，仅对正链正确；对负链（tss=end、tes=start）应取 `up=[tss,tss+100k]`（5'侧高坐标）、`dn=[tes-100k,tes]`（3'侧低坐标），而代码仍取 `[tss-100k,tss]`/`[tes,tes+100k]`，结果 up≈3'侧、dn≈5'侧，**标签互换**（实证：负链基因 OR4F29 start=450740/end=451678，代码 up=[351678,451678]、dn=[450740,550740]，正确应为 up=[451678,551678]、dn=[350740,450740]）。约半数基因受影响，使"上/下游 vs 基因体"的窗口特异性结论部分失效（±1Mb 与 body 窗口不受影响，总体关联结论仍成立）。 | 负链交换 up/dn 的计算方向，重跑并核对窗口分布。 |
| 低 | `run_sv_ewas.py:74–78` / `run_interaction.py:40–44` / `ewas_engine.py:27–29` | 甲基化缺失值按位点均值填补（mean impute），会低估方差、使 SE 偏小。 | 记录缺失率；可选加权或敏感分析。 |
| 低 | `ewas_engine.py` | 是 case-control（Status 主效应）旧版，已被 `run_sv_ewas.py`（SV 窗口主效应）取代；同名遗留易混淆。 | 标注 deprecated 或删除。 |
| 低 | `check_ksr1p1.py:3,8` | 脚本把 KSR1P1 区域硬编码为 `chr1:25.6M-27.6M`，但 KSR1P1（ENSG00000229485）实为 **chr10:42,149,310–42,149,549**（已从 `genes_grch38.tsv` 核实），且是 `processed_pseudogene`（不在当前 protein_coding 的 `genes_windows.tsv` 中）。该验证脚本按错误染色体过滤，输出为空/误导。 | 修正为 chr10:42.0–42.3M，并注意 KSR1P1 已被 protein_coding 过滤排除。 |
| 中 | `interaction_significant.tsv` | 实测 5,470 行，但 `SV_METHYLATION_RESULTS.md` 声称"119 对交互显著"；且全工作区**无任何脚本生成该文件**（仅 `make_report_figures.py` 读取它）。5,470/6,579=83% 占比过高，暗示阈值口径并非文档所述 FDR<10%，生成步骤缺失。 | 补充交互 FDR 的落地脚本并注释阈值；重建/核对 `interaction_significant.tsv`。 |
| 低 | `extract_matrix_header.ps1:1` | 硬编码 `E:\甲基化数据矩阵\EWAS_INPUT_NO_HEADER.txt`；`dump_clinical.ps1` 等同样硬编码 D: 路径。 | 参数化。 |
| — | `fdr_select.py` | BH-FDR 计算（`cummin` 反向累积）**验证正确**。 | — |
| — | `run_interaction.py:70,77` | 用 `pinv` 处理奇异性、四格 ≥2 的可估性判断、dof=n-10 均正确。 | — |
| — | `dump_clinical.ps1` | 该 sheet XML 单元格仅有 `inlineStr`/`n`（无共享字符串），解析逻辑对该文件正确。 | 若换用含共享字符串的 xlsx 需补 `t="s"` 处理。 |

---

## 跨模块共性问题

1. **硬编码绝对路径泛滥**：`E:\`、`/mnt/e/`、`/mnt/d/`、`D:/`、`C:/Users/Administrator/Desktop/` 混用，且 E: 盘数据在工作区外，换机无法复现。
2. **编码 bug 实锤**：工作区存在乱码目录 `鐢插熀鍖栭娴嬭浆褰曠粍`（`甲基化预测转录组` 的 GBK/UTF-8 错码副本），内含 26 个 IDAT、约 **339 MB** 重复数据，系下载管线编码问题所致。
3. **索引/坐标体系依赖隐式约定**：sv_idx 顺序索引、hg38/hg19 坐标、0/1-based 等关键约定多处靠"碰巧一致"维持，缺乏显式断言，静默错位风险高。
4. **版本管理缺失**：`figure2` 虽有 `.git`，但本次审查环境 `git` 被沙箱拒绝访问，无法核对代码变更历史与文档脱节的时点。

---

## TOP 问题清单（按优先级）

1. 【高】`SV_METHYLATION_RESULTS.md` 与当前代码/产物全面脱节（protein_coding 过滤未同步文档）——模块3 结论不可复现。
2. 【高】`make_gene_windows.py` 负链基因 up/down 窗口颠倒——模块3 窗口特异性结论部分失效。
3. 【高】`gsmap_healthcheck.py` hg38→hg19 未 liftover，rsID 映射错位——模块2 早期 sumstats 产物错误。
4. 【中】`meta_rpl_ea.py` 权重口径不一致 + 效应等位基因/样本量硬编码未验证。
5. 【中】`calc_mean_chisq_{abortion,pval}.py` 中位数 `total=n+overflow` 双重计数。
6. 【中】CN1→GRCh38 liftover 丢失 ~34% 断点，未统计/未说明。
7. 【中】sv_idx 顺序索引对齐脆弱（隐式约定，易静默错位）。
8. 【中】`extract_sv_carriers.py` 缺失 GT 静默判为非携带者。
9. 【低】`build_boosted_sumstats.py` 合成信号(Z=8)未隔离，有被误用为真实证据的风险。
10. 【低】大量硬编码路径 + 乱码重复目录 + 数据在 E: 盘。

> 附：`paf2chain.py` 曾被认为 liftover 方向/gap 记账可能颠倒，经实证（CN1 chr1:65,913,036 → GRCh38 chr1:65,590,242，偏移与 chain 坐标差一致）**确认方向与 gap 记账正确**，不在问题清单内。

---

*本报告基于对三个模块全部代码文件的逐行阅读，以及对关键产物（`genes_windows.tsv`、`gene_window_patients.tsv`、`sv_ewas_results.tsv`、`sv_ewas_pvals.npy`、`sv_ewas_sig_pairs.tsv`、`sv_ewas_selected_sites.tsv`、`sv_carriers.tsv`、`GRCh38_breakpoints.unique.bed`、临床 sheet XML）的实际核验。*
